import Stripe from 'stripe'
import { env } from '../env'

export const stripe = new Stripe(env().STRIPE_SECRET_KEY, {
  // SDK v17 default. Pinning explicitly so an SDK upgrade doesn't change behavior.
  apiVersion: '2024-09-30.acacia',
  typescript: true,
  appInfo: { name: 'Postari', version: '0.1.0', url: 'https://postari.jp' },
})
